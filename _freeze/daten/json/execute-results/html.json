{
  "hash": "d8bd3c78c3a31bf15eb4374c3533a85d",
  "result": {
    "engine": "jupyter",
    "markdown": "# JSON-Dateien\n\nJSON (JavaScript Object Notation) ist ein weit verbreitetes, textbasiertes Datenformat, das sich durch seine einfache Lesbarkeit und Schreibbarkeit auszeichnet. Es eignet sich hervorragend für den Datenaustausch zwischen verschiedenen Systemen und Programmiersprachen.\n\nJSON-Daten bestehen aus Schlüssel-Wert-Paaren und unterstützen folgende Datentypen:\n\n1. Zeichenketten (Strings)\n2. Zahlen\n3. Boolesche Werte (true/false)\n4. null\n5. Arrays (Listen)\n6. Objekte (ähnlich wie Dictionaries in Python)\n\nBeispiel einer JSON-Datei:\n\n```json\n{\n    \"Personen\": [\n        {\n            \"Name\": \"Max\",\n            \"Alter\": 24,\n            \"Stadt\": \"Berlin\"\n        },\n        {\n            \"Name\": \"Anna\",\n            \"Alter\": 30,\n            \"Stadt\": \"Hamburg\"\n        },\n        {\n            \"Name\": \"Tom\",\n            \"Alter\": 22,\n            \"Stadt\": \"München\"\n        }\n    ]\n}\n```\n\nIn diesem Beispiel enthält die JSON-Datei ein Objekt mit dem Schlüssel \"Personen\", der eine Liste von Objekten speichert. Jedes dieser Objekte repräsentiert eine Person mit den Attributen \"Name\", \"Alter\" und \"Stadt\".\n\n\n## Schreiben und Lesen einer JSON-Datei\n\nDas Speichern von Daten in eine JSON-Datei erfolgt mithilfe der `json`-Bibliothek. Die Methode `json.dump()` ermöglicht das Speichern eines Python-Dictionaries in eine JSON-Datei.\n\n::: {#25b02bcf .cell execution_count=1}\n``` {.python .cell-code}\nimport json\n# Daten erstellen\ndaten = {\n    \"Personen\": [\n        {\"Name\": \"Max\", \"Alter\": 24, \"Stadt\": \"Berlin\"},\n        {\"Name\": \"Anna\", \"Alter\": 30, \"Stadt\": \"Hamburg\"},\n        {\"Name\": \"Tom\", \"Alter\": 22, \"Stadt\": \"München\"}\n    ]\n}\n# JSON-Datei speichern\nwith open('daten.json', 'w') as jsonfile:\n    json.dump(daten, jsonfile, indent=4)\n```\n:::\n\n\n:::{.callout-note collapse=\"true\" title=\"Detaillierte Erklärung\"}\n\n1. Importieren der JSON-Bibliothek:\n   ```python\n   import json\n   ```\n   Die JSON-Bibliothek ist in Python integriert und muss nicht separat installiert werden.\n\n2. Erstellen der Daten:\n   Die Daten werden als verschachteltes Dictionary angelegt, das die Struktur der gewünschten JSON-Datei widerspiegelt.\n\n3. Speichern der Daten:\n   ```python\n   with open('daten.json', 'w') as jsonfile:\n       json.dump(daten, jsonfile, indent=4)\n   ```\n   - Der `with`-Befehl stellt sicher, dass die Datei nach dem Schreibvorgang automatisch geschlossen wird.\n   - `'daten.json'` ist der Name der zu erstellenden Datei.\n   - `'w'` öffnet die Datei im Schreibmodus (write).\n   - `json.dump()` konvertiert das Python-Dictionary in JSON-Format und schreibt es in die Datei.\n   - `indent=4` sorgt für eine formatierte, leicht lesbare Ausgabe mit Einrückungen.\n\n:::\n\nDas **Einlesen** von JSON-Dateien in Python erfolgt ebenfalls mit der `json`-Bibliothek. Die Methode `json.load()` wandelt den Inhalt einer JSON-Datei in ein Python-Dictionary um.\n\n::: {#3729f547 .cell execution_count=2}\n``` {.python .cell-code}\nimport json\n\n# JSON-Datei lesen\nwith open('daten.json', 'r') as jsonfile:\n    daten = json.load(jsonfile)\n\nprint(daten)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n{'Personen': [{'Name': 'Max', 'Alter': 24, 'Stadt': 'Berlin'}, {'Name': 'Anna', 'Alter': 30, 'Stadt': 'Hamburg'}, {'Name': 'Tom', 'Alter': 22, 'Stadt': 'München'}]}\n```\n:::\n:::\n\n\n:::{.callout-note collapse=\"true\" title=\"Detaillierte Erklärung\"}\n\n1. Öffnen und Lesen der JSON-Datei:\n   ```python\n   with open('daten.json', 'r') as jsonfile:\n       daten = json.load(jsonfile)\n   ```\n   - `'daten.json'` ist der Name der zu lesenden Datei.\n   - `'r'` öffnet die Datei im Lesemodus (read).\n   - `json.load()` liest den Inhalt der Datei und konvertiert ihn in ein Python-Dictionary.\n\n2. Ausgabe der gelesenen Daten:\n   ```python\n   print(daten)\n   ```\n   Dies zeigt den Inhalt des eingelesenen Dictionaries an und ermöglicht eine Überprüfung der korrekten Datenstruktur.\n\n:::\n\n## JSON-Daten in Python bearbeiten\n\nJSON lässt sich einfach in Python-Datenstrukturen umwandeln und umgekehrt.\n\n### JSON zu Dictionary\n\nJSON in ein Python Dictionary konvertieren:\n\n::: {#9cb2fa87 .cell execution_count=3}\n``` {.python .cell-code}\nimport json\n\njson_daten = '''\n{\n    \"Name\": \"Max\",\n    \"Alter\": 24,\n    \"Stadt\": \"Berlin\"\n}\n'''\n\ndaten = json.loads(json_daten)\nprint(daten)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n{'Name': 'Max', 'Alter': 24, 'Stadt': 'Berlin'}\n```\n:::\n:::\n\n\n### Dictionary zu JSON\n\nPython-Dictionary in JSON konvertieren:\n\n::: {#274274b1 .cell execution_count=4}\n``` {.python .cell-code}\nimport json\n\ndaten = {\n    \"Name\": \"Max\",\n    \"Alter\": 24,\n    \"Stadt\": \"Berlin\"\n}\n\njson_daten = json.dumps(daten, indent=4)\nprint(json_daten)\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n{\n    \"Name\": \"Max\",\n    \"Alter\": 24,\n    \"Stadt\": \"Berlin\"\n}\n```\n:::\n:::\n\n\n<!-- \nJSON-Daten können komplexe, verschachtelte Strukturen enthalten, wie Listen von Objekten oder Objekte innerhalb von Objekten. Python ermöglicht eine direkte Verarbeitung dieser Strukturen.\n\n::: {#6173f799 .cell execution_count=5}\n``` {.python .cell-code}\nimport json\n\njson_daten = '''\n{\n    \"Personen\": [\n        {\n            \"Name\": \"Max\",\n            \"Alter\": 24,\n            \"Stadt\": \"Berlin\",\n            \"Hobbys\": [\"Lesen\", \"Schwimmen\"]\n        },\n        {\n            \"Name\": \"Anna\",\n            \"Alter\": 30,\n            \"Stadt\": \"Hamburg\",\n            \"Hobbys\": [\"Radfahren\", \"Kochen\"]\n        }\n    ]\n}\n'''\n\ndaten = json.loads(json_daten)\n\n# Zugriff auf verschachtelte Daten\nfor person in daten[\"Personen\"]:\n    print(f\"Name: {person['Name']}, Hobbys: {', '.join(person['Hobbys'])}\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nName: Max, Hobbys: Lesen, Schwimmen\nName: Anna, Hobbys: Radfahren, Kochen\n```\n:::\n:::\n\n\nDieses Beispiel demonstriert, wie auf verschachtelte Datenstrukturen in JSON zugegriffen und wie diese verarbeitet werden können. -->\n\n",
    "supporting": [
      "json_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}